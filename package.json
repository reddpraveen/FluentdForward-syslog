{
  "name": "openshift-manifest-viewer",
  "version": "1.0.0",
  "description": "A UI for reading OpenShift manifests via REST API",
  "main": "app.js",
  "scripts": {
    "start": "node app.js"
  },
  "dependencies": {
    "@kubernetes/client-node": "^0.18.0",
    "express": "^4.18.1",
    "js-yaml": "^4.1.0"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
